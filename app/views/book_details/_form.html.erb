<%= form_for(@book_detail) do |f| %>
  <% if @book_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_detail.errors.count, "error") %> prohibited this book_detail from being saved:</h2>

      <ul>
      <% @book_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :detail %><br />
    <%= f.text_field :detail %>
  </div>
  <div class="field">
    <%= f.label :book %><br />
    <%= f.text_field :book %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
